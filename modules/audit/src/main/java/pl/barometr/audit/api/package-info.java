/**
 * What audit publishes: one verb.
 *
 * Recording something is all another part of the system may do — not reading, not
 * correcting, not deleting. An audit trail that could be read back by whatever writes
 * to it is one more place for a mistake to be quietly repaired.
 *
 * A Java file in a Kotlin module because Kotlin has no package-level annotations.
 */
@org.springframework.modulith.NamedInterface("api")
package pl.barometr.audit.api;
