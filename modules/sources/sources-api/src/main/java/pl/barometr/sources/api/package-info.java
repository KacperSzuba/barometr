/**
 * The published contract of the sources module: the registry of everything the
 * system ingests from, together with the legal basis it does so on.
 */
@org.springframework.modulith.NamedInterface("api")
package pl.barometr.sources.api;
